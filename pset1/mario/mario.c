#include <stdio.h>
#include <cs50.h>

void mario(void)
{
    do 
    int input = get_int("Pick a number 1-8\n");
    if (input >= 1 && input <= 8)
    {
        // for (let i=0; i < input; i++)
        // {
        //     printf("#\n")
        // }

        printf("%i\n", input);
    }
}
